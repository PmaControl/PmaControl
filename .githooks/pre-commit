#!/usr/bin/env bash

set -euo pipefail

ROOT_DIR="$(git rev-parse --show-toplevel)"
PHPUNIT_BIN="$ROOT_DIR/vendor/bin/phpunit"

if [[ ! -x "$PHPUNIT_BIN" ]]; then
    echo "pre-commit: phpunit not found at $PHPUNIT_BIN" >&2
    exit 1
fi

echo "pre-commit: running phpunit"
"$PHPUNIT_BIN"
