# Decoupage

- Type: trait
- Namespace: `App\Library`
- Source: `App/Library/Decoupage.php`

- `OnAddServer($param)`: Handle decoupage state through `OnAddServer`.
- `OnRemServer($param)`: Handle decoupage state through `OnRemServer`.
- `install()`: Handle decoupage state through `install`.
- `buildRootTable($name_table, $table_link)`: Handle decoupage state through `buildRootTable`.
- `buildTableName($name_table)`: Handle decoupage state through `buildTableName`.
- `buildTableLink($name_table, $table_link)`: Handle decoupage state through `buildTableLink`.
- `buildTablePartition($name_table, $table_link, $id_server)`: Handle decoupage state through `buildTablePartition`.
- `buildMainTable($prefix, $table_link, $id_servers)`: Handle decoupage state through `buildMainTable`.
- `buildTableHistory($prefix, $table_link, $id_servers)`: Handle decoupage state through `buildTableHistory`.
- `getIdservers()`: Retrieve decoupage state through `getIdservers`.
- `tryRocksDb()`: Handle decoupage state through `tryRocksDb`.
- `rebuildAll($param)`: Handle decoupage state through `rebuildAll`.
- `init($param)`: Handle decoupage state through `init`.
- `dropAll($param)`: Handle decoupage state through `dropAll`.
- `init_variables()`: Handle decoupage state through `init_variables`.
