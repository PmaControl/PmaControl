# Display

- Type: class
- Namespace: `App\Library`
- Source: `App/Library/Display.php`

- `server($arr)`: Handle display state through `server`.
- `srv($id_mysql_server, $withip, $url)`: Handle display state through `srv`.
- `srvjs($id_mysql_server)`: Handle display state through `srvjs`.
- `icon($icon)`: Handle display state through `icon`.
- `icon32($icon)`: Handle display state through `icon32`.
- `ts_variable($id_ts_variable)`: Handle display state through `ts_variable`.
