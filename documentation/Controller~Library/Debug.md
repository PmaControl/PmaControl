# Debug

- Type: class
- Namespace: `App\Library`
- Source: `App/Library/Debug.php`

- `parseDebug($param)`: Handle debug state through `parseDebug`.
- `debugShowQueries($di_link)`: Handle debug state through `debugShowQueries`.
- `checkPoint($name)`: Handle debug state through `checkPoint`.
- `debugShowTime()`: Handle debug state through `debugShowTime`.
- `debugPurge()`: Handle debug state through `debugPurge`.
- `debugQueriesOff()`: Handle debug state through `debugQueriesOff`.
- `debug($string, $var, $font_color, $background_color)`: Handle debug state through `debug`.
- `sql($sql, $var)`: Handle debug state through `sql`.
- `getDate()`: Retrieve debug state through `getDate`.
- `warning($string, $var)`: Handle debug state through `warning`.
- `error($string, $var)`: Handle debug state through `error`.
- `success($string, $var)`: Handle debug state through `success`.
- `head()`: Handle debug state through `head`.
