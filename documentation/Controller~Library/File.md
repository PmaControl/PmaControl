# File

- Type: trait
- Namespace: `App\Library`
- Source: `App/Library/File.php`

- `compressAndCrypt($file)`: Handle file state through `compressAndCrypt`.
- `getFileinfo($filename)`: Retrieve file state through `getFileinfo`.
- `cryptFile($file_name)`: Handle file state through `cryptFile`.
- `decryptFile($file_name)`: Handle file state through `decryptFile`.
- `compressFile($path_file)`: Handle file state through `compressFile`.
- `unCompressFile($path_file)`: Handle file state through `unCompressFile`.
- `unCompressAndUnCrypt($file_uncrypted, $is_crypted)`: Handle file state through `unCompressAndUnCrypt`.
