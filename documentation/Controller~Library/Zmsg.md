# Zmsg

- Type: class
- Namespace: `App\Library`
- Source: `App/Library/Zmsg.php`

- `__construct($socket)`: Handle zmsg state through `__construct`.
- `s_encode_uuid($data)`: Handle zmsg state through `s_encode_uuid`.
- `s_decode_uuid($data)`: Handle zmsg state through `s_decode_uuid`.
- `set_socket($socket)`: Handle zmsg state through `set_socket`.
- `recv()`: Handle zmsg state through `recv`.
- `send($clear)`: Handle zmsg state through `send`.
- `parts()`: Handle zmsg state through `parts`.
- `last()`: Handle zmsg state through `last`.
- `set_last($set)`: Handle zmsg state through `set_last`.
- `body()`: Handle zmsg state through `body`.
- `body_set($body)`: Handle zmsg state through `body_set`.
- `body_fmt()`: Handle zmsg state through `body_fmt`.
- `push($part)`: Handle zmsg state through `push`.
- `pop()`: Handle zmsg state through `pop`.
- `address()`: Create zmsg state through `address`.
- `wrap($address, $delim)`: Handle zmsg state through `wrap`.
- `unwrap()`: Handle zmsg state through `unwrap`.
- `__toString()`: Handle zmsg state through `__toString`.
- `test()`: Handle zmsg state through `test`.
- `save($fh)`: Update zmsg state through `save`.
- `load($fh)`: Handle zmsg state through `load`.
