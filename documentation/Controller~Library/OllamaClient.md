# OllamaClient

- Type: class
- Namespace: ``
- Source: `App/Library/Ollama.php`

- `__construct($endpoint, $model, $timeout)`: Handle ollama client state through `__construct`.
- `generate($prompt, $system)`: Handle ollama client state through `generate`.
