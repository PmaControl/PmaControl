# Dot

- Type: trait
- Namespace: `App\Library`
- Source: `App/Library/Dot.php`

- `generateAllGraph()`: Handle dot state through `generateAllGraph`.
- `generateGraph($group)`: Handle dot state through `generateGraph`.
- `nodeHead($display_name, $id_mysql_server)`: Handle dot state through `nodeHead`.
- `nodeLine($line)`: Handle dot state through `nodeLine`.
- `generateNode($id_mysql_server)`: Handle dot state through `generateNode`.
- `generateRankForMM($group)`: Handle dot state through `generateRankForMM`.
