# Chiffrement

- Type: class
- Namespace: `App\Library`
- Source: `App/Library/Chiffrement.php`

- `__construct($clef)`: Handle chiffrement state through `__construct`.
- `chiffre($plaintext)`: Handle chiffrement state through `chiffre`.
- `dechiffre($plaintext)`: Handle chiffrement state through `dechiffre`.
- `loop($src, $chiffre)`: Handle chiffrement state through `loop`.
- `chiffre_fichier($src)`: Handle chiffrement state through `chiffre_fichier`.
- `dechiffre_fichier($src)`: Handle chiffrement state through `dechiffre_fichier`.
- `fwrite_stream($fp, $string)`: Handle chiffrement state through `fwrite_stream`.
- `encrypt($msg, $password)`: Handle chiffrement state through `encrypt`.
- `decrypt($password_crypted, $password)`: Handle chiffrement state through `decrypt`.
