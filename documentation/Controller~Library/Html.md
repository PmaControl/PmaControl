# Html

- Type: class
- Namespace: `App\Library`
- Source: `App/Library/Html.php`

- `box($title, $body)`: Handle html state through `box`.
- `table($thead, $tbody)`: Handle html state through `table`.
- `thead($th)`: Handle html state through `thead`.
- `tbody($td)`: Handle html state through `tbody`.
