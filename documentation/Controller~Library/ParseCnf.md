# ParseCnf

- Type: class
- Namespace: `App\Library`
- Source: `App/Library/ParseCnf.php`

- `parseCnf($path, $parsed)`: Handle parse cnf state through `parseCnf`.
- `getCnf($mycnf)`: Retrieve parse cnf state through `getCnf`.
