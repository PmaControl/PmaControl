# Extraction

- Type: class
- Namespace: `App\Library`
- Source: `App/Library/Extraction.php`

- `extract($var, $server, $date, $range, $graph)`: Handle extraction state through `extract`.
- `display($var, $server, $date, $range, $graph)`: Handle extraction state through `display`.
- `getIdVariable($var)`: Retrieve extraction state through `getIdVariable`.
- `count_recursive($array)`: Handle extraction state through `count_recursive`.
- `setOption($var, $val)`: Handle extraction state through `setOption`.
- `getSizeByEngine($data)`: Retrieve extraction state through `getSizeByEngine`.
- `display2($var, $server, $date, $range, $graph)`: Handle extraction state through `display2`.
- `isExist($var)`: Handle extraction state through `isExist`.
