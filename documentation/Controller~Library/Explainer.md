# Explainer

- Type: class
- Namespace: `Rap2hpoutre\MySQLExplainExplain`
- Source: `App/Library/Explainer/Explainer.php`

- `__construct($query, $mysql_version)`: Handle explainer state through `__construct`.
- `addRow($row)`: Create explainer state through `addRow`.
- `getRowsCount()`: Retrieve explainer state through `getRowsCount`.
- `performQueryAnalysis($query)`: Handle explainer state through `performQueryAnalysis`.
- `initExplainCols()`: Handle explainer state through `initExplainCols`.
